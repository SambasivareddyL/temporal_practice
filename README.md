# temporal_practice

Small demo project containing `temporal_approval` placeholders.

Contents:
- temporal_approval/ — workflow, activities, worker, API, runner

Instructions:
- Fill in Temporal worker and workflow logic.
- To initialize git and push, add a remote and run `git push -u origin main`.
